<div class="page-header">
  <h1>Resend Activation Key</h1>
</div>

<%= simple_form_for(:activation, :url => { :action => :create }, :defaults => { :input_html => { :class => 'input-xlarge' } }) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :email %>
  </div>
  
  <div class="form-actions">
    <%= f.submit 'Resend Activation Key', :class => "btn-primary" %>
  </div>
<% end %>
